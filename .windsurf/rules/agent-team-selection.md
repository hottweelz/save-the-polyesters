# Windsurf Adapter: Agent Team Selection

Follow the root `AGENTS.md`.

Agent selection logic lives in:

```txt
.ai/rules/agent-team-selection.md
```

Before non-trivial implementation work:
- read `.ai/rules/agent-team-selection.md`
- inspect `.ai/agents/`
- select the smallest useful team
- state the selected agents before implementation
- Feel free to move on to next obvious steps, only stopping when you feel the task is complete or when you encounter a blocker.
